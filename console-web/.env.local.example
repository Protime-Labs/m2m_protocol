# Copy to .env.local (never committed). Both values are PUBLIC (publishable) --
# reads are gated by Supabase Auth + RLS, writes by the estates' private write key.
NEXT_PUBLIC_SUPABASE_URL=https://<project>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon or sb_publishable_... key>
